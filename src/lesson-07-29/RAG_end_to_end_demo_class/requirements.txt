# ── LangChain Core + Modules ──────────────────────────────────────
langchain>=0.2.14
langchain-core>=0.2.28
langchain-community>=0.2.6
langchain-openai>=0.1.8
langchain-text-splitters>=0.2.1
langchain-chroma>=0.1.2

# ── Vector store & embeddings ─────────────────────────────────────
chromadb>=0.5.5
openai>=1.12.0
rank_bm25>=0.0.2

# ── Evaluation (RAGAS + metrics) ──────────────────────────────────
datasets>=3.0.0
ragas>=0.1.7
tabulate>=0.9.0

# ── Utilities ─────────────────────────────────────────────────────
requests>=2.31.0
pymupdf>=1.24.9     # ✅ Required for PyMuPDFLoader (PDF text extraction)

# ── Optional: local embeddings or rerankers (future extension) ───
sentence-transformers>=2.7.0
transformers>=4.42.0
torch>=2.2.0
